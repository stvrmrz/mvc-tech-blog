{
    "development": {
      "username": "${DB_USER}",
      "password": "${DB_PASSWORD}",
      "database": "${DB_NAME}",
      "host": "${DB_HOST}",
      "dialect": "postgres"
    },
    "test": {
      "username": "${DB_USER}",
      "password": "${DB_PASSWORD}",
      "database": "${DB_NAME_TEST}",
      "host": "${DB_HOST}",
      "dialect": "postgres"
    },
    "production": {
      "use_env_variable": "DATABASE_URL"
    }
  }
  